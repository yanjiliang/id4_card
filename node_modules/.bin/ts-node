#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@isaacs/ts-node-temp-fork-for-pr-2009/dist/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../@isaacs/ts-node-temp-fork-for-pr-2009/dist/bin.js" "$@"
  ret=$?
fi
exit $ret
